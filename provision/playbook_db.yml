---
- hosts: all
  sudo: yes
  
  tasks:
    - name: Update Apt cache
      yum: update_cache=yes
    
    - name: Install git
      yum: pkg=git state=present
    
    - name: Clone repo
      git: repo=https://github.com/JPPorcel/CCSA.git dest=/opt/app version=HEAD
      
    - name: Install mongo
      shell: 'cd /opt/app/provision/ && cp mongodb-org.repo /etc/yum.repos.d/ && yum repolist && sudo yum install mongodb-org && sudo systemctl start mongod'

    - name: Load database
      shell: "cd /opt/app/mongo && mongoimport --db restaurants --collection restaurants --drop --file restaurantes.json"
